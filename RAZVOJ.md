# Развој и одржавање

Важећи водич за одржавање. За прву измену види и [CONTRIBUTING.md](CONTRIBUTING.md).
Историјски разлози и мерења су у [RAZVOJ-ISTORIJA.md](RAZVOJ-ISTORIJA.md);
стање текућих пакета прати [IZVRSAVANJE-PLANA.md](IZVRSAVANJE-PLANA.md).

## Шта је ово

Вежбаоница за теоријски испит **А категорије** над званичном базом питања (еУправа).
Нема сервера, нема базе података, нема билд корака за саму апликацију: то је обичан
HTML + CSS + један JS фајл. Напредак се чува у `localStorage` прегледача.

## Покретање

| Начин | Како | Напомена |
|---|---|---|
| Најпростије | двоклик на `index.html` | ради директно из фолдера (`file://` режим) |
| Ако нешто заглави | двоклик на `Pokreni.bat` | диже локални сервер на порту 8137 и отвара прегледач |
| Ручно | `node serve.mjs` па `http://localhost:8137` | исто као горе |

**Адресе и историја од v146:** hash адресе (`#/sek/c30`, `#/p/7921`) и
стрелице прегледача раде преко сервера и у `file://` режиму. History API памти
појединачне посете; када није доступан, остаје обично hash рутирање. То не
уклања остала ограничења `file://`, попут service worker-а и заједничке браве.

Локални сервер служи само јавне фајлове апликације и `img/<број>.jpg`.
Извози напретка, `tools/`, `.git/` и остали фајлови нису доступни преко HTTP-а.
Када се додаје јавни ресурс, допунити листу у `serve.mjs` и њену проверу.

**Јавни описи од v147:** `STR` у `app.js`, метаподаци `index.html`, плакат и
README морају описивати исте стварне услове. Не представљати незванична
објашњења као потпуно проверена, посматрани распоред извлачења као гаранцију,
инсталацију као гаранцију очувања напретка или offline као доступност слика
које још нису преузете. Подаци напретка су локални; GoatCounter и остали
спољни захтеви описани су засебно у README-у.

## Структура

```
index.html          костур странице (све „екране" држи као <section class="view">)
app.js              цела апликација (један IIFE, без зависности)
style.css           стилови, укључујући тамну тему (body.dark) и широке екране (≥1200px)
data.js             ГЕНЕРИСАНО — питања, области, подобласти
image-baseline.js   ЗАМРЗНУТО — отисци старих слика за већ отворене табове
explanations.js     ГЕНЕРИСАНО — објашњења по питању + картице појмовника
version.js          број верзије (за обавештење „стигла нова верзија")
img/                слике питања (704 комада)
serve.mjs           мали статички сервер за локални развој
tools/              алати за освежавање података (види доле)
tools/arhiva/       примењене једнократне закрпе — историја, не покретати поново
```

### Екрани

Сваки екран је `<section id="view-XXX" class="view">`. Приказује се додавањем
класе `active` (`show('browse')` у `app.js`).

> **Замка:** ако у CSS-у пишеш `#view-home { display: … }`, то ће надјачати
> `.view { display: none }` и екран ће остати видљив преко свега. Увек пиши
> `#view-home.active { display: … }`. Провера:
> ```bash
> grep -oE '#view-[a-z]+\s*\{[^}]*display:' style.css | grep -v '\.active'
> ```
> (излаз мора бити празан)

## Ток података

```
еУправа API ──harvest.mjs──> tools/base-A.json ──build-data.mjs──> data.js
                                    │
                                    └──download-images.mjs──> img/

ручно писани текстови у tools/build-explanations.mjs ──build-explanations.mjs──> explanations.js
```

`practiceId` служи само за захтеве при преузимању: `harvest.mjs` га не уписује
у јавни JSON. Не додавати личне идентификаторе, приступне податке или извозе
напретка у репозиторијум. Чишћење текућег фајла не брише старе Git ревизије.

Провере јавних фајлова и санитаризације (синтетички подаци, без портала/Chrome-а):

```bash
node --test tools/tests/source-privacy.test.mjs tools/tests/server-public-files.test.mjs
```

### Алати (`tools/`)

| Алат | Чему служи |
|---|---|
| `harvest.mjs <guid> <lang> <out>` | преузима базу питања са еУправе (треба важећи GUID за вежбање) |
| `build-data.mjs [--lat фајл] [--cyr фајл] [--images фасцикла] [--out фајл] [--date YYYY-MM-DD]` | спаја проверена два писма у `data.js` |
| `download-images.mjs --guid GUID [--source base.json] [--out img] [--refresh]` | преузима слике питања; подразумевано `tools/base-A.json` → `img/` |
| `build-explanations.mjs` | од извора у самом фајлу прави `explanations.js` |
| `bump-version.mjs` | подиже број верзије и ознаке у `index.html` |
| `patch-srpski.mjs` | једнократна језичка исправка (задржано ради историје) |

`harvest.mjs` најпре преузима и проверава цео скуп одговора, па тек онда
замењује излаз. Један неуспео захтев, неважећи JSON, поновљени ID, погрешан
број одговора или неслагање са изричитим API бројачем заустављају замену.
Ваљане празне подобласти остају дозвољене. Сваки захтев има највише три
покушаја по 30 секунди; приватни GUID се не исписује и не улази у извор.
Подразумевано пише `base.json` у радној фасцикли, језик 15; језик 9 је ћирилица.
Без очекиваног броја из API-ја не може се доказати да је сервер послао баш сва
питања: унутрашња сагласност није доказ потпуности портала. Не уписивати 1.327
као универзални број будућих верзија базе. Провера без портала:
`node --test tools/tests/harvest-integrity.test.mjs tools/tests/source-privacy.test.mjs`.

`build-data.mjs` проверава оба извора истим валидатором, а затим њихову
међусобну сагласност: ID-јеве, области/подобласти, бодове, тачне изборе,
обавезан број одговора, ознаке слика и непразне текстове на оба писма.
Потребне слике морају постојати као непразни фајлови; то није провера њиховог
значења. Од v157 builder израчунава њихове SHA-256 отиске у `imageHashes`.
Све провере претходе припреми/замени излаза. Подразумеване путање
припадају пројекту, експлицитне радној фасцикли. `--date` даје поновљив датум
генерисања; без њега узима се дан изградње у UTC-у. То **није** датум провере
портала или прописа. Провера: `node --test tools/tests/build-data.test.mjs`.

Затечена разлика при обнови v139 из сирових извора: 1.112 питања има другачији
редослед понуђених одговора од праћеног `data.js`, уз исте ID-јеве и вредности.
То је постојало и са старим алатом. Builder задржава редослед латиничног извора;
не зависити од старог излаза ради прикривања разлике. При следећој стварној обнови
базе посебно прегледати diff и поновити испит/историју. Исправка алата није
регенерисала питања. Миграција v157 додала је само мапу отисака у постојећи
јавни излаз, чувајући дословно сва остала поља и редоследе.

Уредничка исправка v173 упарила је 37 ћириличних вредности у `data.js` са
45 вредности у `tools/base-A-cyr.json` (осам су поновљени називи подобласти).
Исправљена су помешана слова и дословни кодови N/T/TWI, уз тачну проверу
стабилних ID-јева и непромењених осталих бајтова. Пуна регенерација није
коришћена да мала словна исправка не би успут променила редоследе одговора.
Доказ и граница провере: `docs/revizija-sadrzaja/grupe/021-ispravke-pisma.md`.

Путање алата за слике без аргумената везане су за пројекат; експлицитне путање
су релативне радној фасцикли. Извор се у целини проверава пре мреже и уписа.
Већ преузета слика са исправним JPEG почетком/крајем прескаче се, осим уз
`--refresh`. Нови фајл се прво припрема поред одредишта, па тек онда замењује
стари. HTTP грешка, HTML или прекинут JPEG остављају стару слику и дају
неуспешан излазни код; GUID се не исписује. Ово није провера значења или
потпуно декодирање слике. Пре јавне објаве замењених слика поново направити
`data.js`, прегледати diff, освежити садржајну евиденцију и подићи број издања.
`verify.mjs` одбија мапу која не одговара стварним бајтовима. Не мењати ручно
кључ кеша или историјски `image-baseline.js` при обичној обнови слика.
Провера без портала: `node --test tools/tests/download-images.test.mjs`.

**После сваке измене** `app.js`/`style.css`/`index.html` покрени:

```bash
cd tools && node bump-version.mjs
```

Алтернативно, из корена: `node tools/bump-version.mjs`. Путање се рачунају
према скрипти. Пре уписа проверавају се оба фајла и свих пет ознака; неисправан
улаз их оставља нетакнутим. Нови садржаји се најпре припремају у привременим
фајловима. Замена два фајла није једна трансакција: после прекида између замена
вратити усклађен пар `version.js`/`index.html` из истог commit-а па поновити.
Провера: `node --test tools/tests/bump-version.test.mjs`.

Тиме се мења `version.js` и `?v=` ознаке у `index.html`, па прегледач узима нове
фајлове, а отворени табови добијају траку „Стигла је нова верзија".

## Конвенције

### Једна локална команда и GitHub провере

Из корена пројекта: `node tools/verify.mjs` (Node.js 22 или новији).
Иста скрипта ради и ако се позове пуном путањом из друге фасцикле. Проверава
верзионисане ресурсе, синтаксу активних скрипти, све `tools/tests/*.test.mjs`
и актуелност евиденције садржаја. Не покреће генераторе/архивиране закрпе,
не отвара Chrome, не приступа порталу и не мења број апликације. Тестови
користе привремене синтетичке пројекте и локалне сервере.

`.github/workflows/verify.yml` покреће исту команду на Linux-у и Windows-у
за push/PR. Actions су везане за конкретне commit-е; дозвола је само читање
репозиторијума. Workflow нема објављивање сајта. Пад зауставља проверу, а
пролаз **не значи** да су browser токови или сва питања семантички проверени.
Њихове засебне команде и ограничења су у одељку провера испод.

### Двојно писмо (латиница + ћирилица)

Сваки текст видљив кориснику постоји као пар `{ l: 'latinica', c: 'ћирилица' }`.
У `app.js` то су уноси у објекту `STR`, а испис иде кроз `L('kljuc')`.

Објашњења се **не пишу двапут**: у `build-explanations.mjs` пише се само латиница,
а ћирилица се добија аутоматским пресловљавањем при билду. Пресловљавач чува
ознаке које морају остати латиничне (`km/h`, `kW`, `cm³`, `mg/ml`, `kg`, категорије
`AM`/`A1`/`A2`/`A`/`B`, натписи на знаковима `STOP`, `BUS`).

> **Провера после билда:** излаз мора бити празан.
> ```bash
> node -e "const s=require('fs').readFileSync('explanations.js','utf8');console.log((s.match(/[A-Za-zčćžšđ][Ѐ-ӿ]|[Ѐ-ӿ][a-zčćžšđ]/g)||[]).filter(t=>!/^n/.test(t)))"
> ```

### Уређивање `tools/build-explanations.mjs`

Фајл садржи **NUL бајтове** (користе се као граничници при пресловљавању), па
уобичајени алати за уређивање знају да га оштете. Мењај га **скриптом** која ради
`split`/`join` над тачним низом и гласно пада ако низ није нађен тачно једном —
образац видљив у `tools/arhiva/`.

### Провера садржаја (научено на грешкама)

Садржајни налази се сада воде по ID-ју у
[евиденцији ревизије](docs/revizija-sadrzaja/METODOLOGIJA.md).
[Група 002](docs/revizija-sadrzaja/grupe/002-prvenstvo-i-brzine.md) показује
поступак за измену заједничке картице: она поново отвара зависна питања,
али им не додељује одобрење. Новији пропис провери и када званични PDF на
сајту министарства касни: за сигнализацију је било потребно допунити текст
кроз 2021. консолидацијама 2024. и 2026. и изворним изменама PIS-а.

[Група 004](docs/revizija-sadrzaja/grupe/004-brzine-potpun-pregled-kartice.md)
бележи потпун преглед картице брзина и свих 15 зависних питања. Посебна CLI
провера `tools/tests/brzine-card.browser.js` мери боје знакова, контраст,
два писма и стварно заклањање цртежа навигацијом. Покреће се само на
изолованом localhost-у и прави нове контексте. Њених 200% односи се на
основни текст странице; натписи SVG-а имају сопствену величину. Успешна
геометријска провера не потврђује смисао осталих картица.

**Плакат од v149:** `plakat.html` има засебан print распоред за једну A4
страну са маргинама 14 mm. После измене текста проверити стварни PDF,
комплетно подножје и QR, уз искључена заглавља/подножја прегледача.
Снимак екрана није доказ броја одштампаних страна.

**Дуге адресе од v152:** линкови у `.trustList` користе `overflow-wrap:
anywhere` и `var(--blue)`. Провера `mobile-layout.browser.js` отвара FAQ на
320px са текстом од 30px, мери ширину документа и контраст нормалног/hover
линка, и проверава да су оригинална адреса, текст и подвлачење очувани.
Навигациони селектори обухватају и дугмад и стварне линкове. Сценарио без
ResizeObserver-а мора имати свој нови BrowserContext због једног писца.

Сваку **визуелну** тврдњу (боја, облик, положај руке на знаку) провери **отварањем саме слике**
`img/<qId>.jpg` — текстуална провера не хвата грешке у бојама. Тако је нађено да је картица
tврдила да је почетак аутопута плава табла, а он је **зелен** (слике 9155/9083).

### Објашњења

- Свако објашњење креће од **тачног одговора баш тог питања**, па објашњава зашто.
- Замке (примамљиви нетачни одговори) се именују и разрешавају.
- Чланови закона се наводе **само после дословне провере** у тексту закона.
- Казнене мере: када су износи битни за питање, раздвојити износ који очекује
  сачувана база од важећег прописа, уз проверени члан и извор. Неслагање не
  прикривати променом званичног кључа; пратити га у појединачној евиденцији.
- Где се званични одговор разилази са важећим законом, текст то каже поштено
   („у испитној бази…"), без измишљања.

## Симулација испита

`SIM_SLOTS` у `app.js` описује шаблон изведен из **шест посматраних званичних
извлачења**: 41 питање, 98 поена, 45 минута, праг 84 поена (85%). То није
потврда шаблона сваког будућег испита.
Распоред по областима је фиксан (нпр. Правила 19 питања/46 поена, Сигнализација
13/34, Последице 0), а питања се бирају насумично унутар сваке ћелије.

Симулација је намерно **верна испиту** — без помоћи којих на испиту нема
(нема објашњења док траје, нема мреже питања, увек светла тема).

## Провера пре објављивања измене

**Историја симулација (v130):** ID изабраног одговора проверава се међу
одговорима тог питања, не међу ID-јевима питања. Збир и преглед по областима
проверавају се на сагласност; ако стари детаљи не одговарају сачуваном резултату,
приказује се објашњење и сачувана листа погрешних питања. Раније избрисане
изборе није могуће реконструисати. Празан стари избор зато не доказује да
кандидат није одговорио. Провера: `node --test tools/tests/simulation-history.test.mjs`.

За сваку измену која дира УЧЕЊЕ, РЕД ПОНАВЉАЊА или СИМУЛАЦИЈУ обавезно и
аутоматска провера бодовања. Користи **засебан тестни профил**, један таб на
`http://localhost:<тестни порт>`, без повезаног фајла за резерву. Не покретати
над профилом у коме неко учи. У конзоли учитати `tools/provera-bodovanja.js`:

1. `await proveraBodovanja()` — чува оба оригинална записа и освежава страницу.
2. Поново учитати скрипту, па `await proveraBodovanja2()` — извршава проверу,
   враћа оригинале и поново освежава страницу. Укључити „Preserve log”.
3. Поново учитати скрипту, па `await proveraBodovanjaPotvrdi()` — проверава
   стварно учитан профил и тек тада уклања резервни контекст.

После прекида: `await proveraBodovanjaVrati()`, па после освежавања трећи корак.
Ако потврда одбије опоравак, не брисати `provera.kontekst` нити затварати таб:
копија остаје у `sessionStorage` за поновни покушај/ручни опоравак. Потврда се
обавља пре даљег коришћења странице; каснија измена намерно задржава копију.
Све ставке пре јавног објављивања треба да буду PASS (број пише у „SVE PROŠLO”),
укључујући симулацију 41/41 → 98/98. Појединачне исправке на развојној грани могу
имати документоване раније кварове који се решавају у наредном пакету.
Сам поступак опоравка проверава `node --test tools/tests/scoring-harness-safety.test.mjs`.
Цео поступак са три стварна освежавања аутоматизује CLI
`tools/tests/scoring.browser.js` (исти засебан localhost:18764 поступак испод).
Ствара нови контекст, користи само синтетички напредак/испит и захтева тачан
повратак оба записа пре успешног резултата. Изворна CLI страница није тестни профил.

Дневни циљ има и проверу са **стварним освежавањем** после сваког корака
(auto → датум → ручни режим → ручна квота, уз план само за понављање):

```bash
npx --yes --package @playwright/cli playwright-cli -s=provera-cilja open http://localhost:18764
npx --yes --package @playwright/cli playwright-cli -s=provera-cilja run-code --filename tools/tests/daily-goal.browser.js
npx --yes --package @playwright/cli playwright-cli -s=provera-cilja close
```

Претходно покренути засебан сервер радне копије на том порту (`PORT=18764`).
Скрипта ствара нови контекст са синтетичким подацима и затвара га у `finally`.
Поређује сачувани план, живо стање, квоте, контроле и текстове пре/после reload-а.
Склапање подешавања остаје намерно по сесији; није услов да остану отворена после reload-а.

За тастатуру, увећање слика/цртежа и копирање броја питања исти CLI поступак
покреће `tools/tests/keyboard.browser.js`. Провера такође ствара нови контекст.
Дијалог прати [W3C образац](https://www.w3.org/WAI/ARIA/apg/patterns/dialog-modal/):
фокус унутра, Tab/Shift+Tab, Escape, неактивна позадина и повратак фокуса.
Ово је провера тих токова, а не потврда пуне WCAG усаглашености или читача екрана.

Од v133 нечитљив, празан или недоступан запис напретка блокира нормалне уписе,
резерву у фајл и покретање/наставак симулације. `load()` разликује одсутан запис
од поквареног. `save(true)` сме да позове само успешно проверен увоз; неуспео
упис оставља заштиту укључену. Оригинал се може сачувати као текст, без
„поправљања” његових бајтова. Ово не спаја рад из више табова.

Провере: `node --test tools/tests/storage-recovery.test.mjs tools/tests/scoring-harness-safety.test.mjs`
и исти CLI поступак са `tools/tests/storage-recovery.browser.js` (localhost:18764).
Браузерска провера користи синтетички испит и извоз у меморију, без корисничког фајла.

Од v140 један активан писац: `vozackiA.writer` Web Lock се тражи пре читања напретка
и испита. Друга сарађујућа картица остаје само за читање; не преузима браву
сама. Од v150 приказује садржај и нуди „Вежбај у овој картици”: после затварања
прве та изричита радња поново учитава актуелне записе и тражи браву.
`vozackiA.v1` и `vozackiA.sim`, њихови формати
и идентификатори остају исти. Извоз из заустављене картице чува њен напредак
из меморије, а постојећи извозни формат не укључује започету симулацију.

Пре уписа се пореде тачни очекивани бајтови записа; спољна промена или
`storage.clear()` заустављају картицу, тастатурне пречице, тајмер испита и
аутоматски фајл. Потврде и асинхрони поступци поново проверавају право уписа
пре промене стања. При напуштању странице писац се пензионише; живи документ
чека активан упис/прекид IndexedDB трансакције пре изричитог отпуштања браве.
То није гаранција да прегледач наставља асинхрони рад после гашења или пада.

Без Web Locks, укључујући `file:`, приказује се упозорење да се ради само у
једној картици. Поређење записа не обезбеђује атомску заштиту од старог клијента
који не поштује браву. Не обећавати потпуну заштиту мешавине v126 и новог кода.
Провере: `node --test tools/tests/multi-tab.test.mjs` и CLI
`tools/tests/multi-tab.browser.js` на localhost:18764. Browser провера користи
стварне засебне странице, извоз у меморију и синтетички испит; lifecycle део
ручно шаље догађаје и није доказ нативног BFCache понашања сваког прегледача.

Од v134 `readiness()` враћа `null` за непознате вероватноће и очекиване поене,
а `spremnost()` излаже `modelDostupan` и број недостајућих питања. Не претварати
`null` у нулу или тачност друге области. Први одговор на свих 1.327 питања
омогућава условни модел, али га не валидира. Број/дани/резултати симулација
приказују се и без модела. Историјски описи испод о „шанси” важе за старе верзије.
Провере: `node --test tools/tests/readiness.test.mjs` и CLI
`tools/tests/readiness.browser.js` (нови контекст, синтетички профили).

Мобилни распоред: CLI `tools/tests/mobile-layout.browser.js`. Резултат је JSON;
`passed: false` или `failureCount > 0` означава пад чак и ако CLI команда заврши
кодом 0. Снимци се чувају у игнорисаном `output/playwright/`. Од v135 висину
доње навигације мери ResizeObserver у `--donjaNavVisina`; подножје и фиксне траке
користе ту меру у мобилном media правилу. Не враћати фиксних 66px, јер натписи
могу да се преломе. Испит задржава своје засебне размаке и боје.
Контраст нормалног текста проверити и при hover-у према
[W3C критеријуму](https://www.w3.org/WAI/WCAG22/Understanding/contrast-minimum.html).

Од v141 постојећи водич чува дугмад видљивим док се дугачак текст помера
у сопственом делу. Положај се поново рачуна при промени величине прозора;
на врло ниском екрану водич користи висину изнад навигације. Нормално затварање
враћа фокус, а `skloni()` при сукобу картица уклања и resize listener без
уписа ознаке завршеног водича или враћања фокуса у закључан садржај.
CLI `tools/tests/tour.browser.js` проверава свих седам корака, поновно отварање,
текст до 200%, промену величине и reload; `tools/tests/tour-lock.browser.js`
проверава сукоб пре/после аутоматског отварања. Подешавања и даље памте
склопљеност само у сесији. Од v145 тест отвара подешавања на стварној краткој
висини, без привременог увећавања прозора. На ширинама до 700px и висинама до
400px горња/доња навигација ван испита помера се са документом, па не прекрива
контроле. Испит задржава свој распоред. `tools/tests/short-viewport.browser.js`
користи текст од 32px на висини 240px (200% основе од 16px, око 213% од 15px).
Независно су проверени и прелази преко границе 400/401px и очувана геометрија
симулације. Није тестиран физички телефон или нативни pinch zoom.

Од v138 провера цртежа мери стварну трансформацију текста (`getScreenCTM`),
уместо накнадног множења односа ширина. Старе процене „на 306px” у историји
испод нису поуздана мерења на телефону, нарочито када SVG има ограничену
ширину или висину. Нови CLI `tools/tests/drawing.browser.js` проверава све
цртеже појмовника, дневни график и картицу уз одговорено питање у стварним
прозорима. Резултат `passed: false` је пад. Праг 10px је постојећи минимум
ове провере, а не WCAG стандард или доказ удобног читања свих корисника.
Од v156 `daily-chart-contrast.browser.js` проверава и контраст дневних натписа,
табеле и стварно отвореног увећања: 21 засебан контекст, оба писма/теме,
320/1280px, текст 125%/200% и празни/нулти/велики резултати. Боје се рачунају
са стварном прозирношћу над позадином; нормалан текст мора имати 4,5:1.
Стубићи и линија прага имају исте податке у натписима, табели и тексту, па
нису једини извор информације; то је изузетак за графику у
[W3C 1.4.11](https://www.w3.org/WAI/WCAG22/Understanding/non-text-contrast.html),
не изузетак за читљивост текста. Провера не потврђује контраст свих цртежа.
Од v159 `footer-links-contrast.browser.js` проверава оба линка подножја,
укључујући приватност: 16 контекста, оба писма/теме, 320/1280px и основни
текст до 32px. Нормалан, hover и фокусирани приказ морају имати најмање
4,5:1, видљив подвучен текст и очувану ширину. CSS користи боје теме за
све линкове подножја; димензије радњи и скривање током испита остају исти.
Уски дијаграми користе постојеће бочне размаке; мале усправне илустрације
чувају однос страна. Нова илустрација мора поново проћи проверу и у појмовнику
и у ужем објашњењу уз питање. Садржај и тачност цртежа припадају засебној А6 ревизији.

Аутоматска резерва од v136 користи `backupZahtev` са handle-ом и ревизијом.
`isprazniRedRezerve()` држи `upisUToku` током уписа и кратког поновног покушаја,
па затим распоређује најновији преостали захтев. Само рани повратак из тајмера
није довољан: тако се губила измена настала током спорог уписа. Сваки писач
мора да се затвори или покуша `abort()`, а грешка старог handle-а не сме да
промени дозволу новог. Провера без стварних фајлова:
`node --test tools/tests/backup-queue.test.mjs`.

Од v143 `backupFaza` описује ажурност фајла: `ceka`, `upisuje`, `uspesno`,
`greska`. Повезан handle није доказ успешног уписа. Потврда важи тек после
`write` + `truncate` + `close`, ако у међувремену није стигао нови захтев.
Стање је само у меморији; формат извоза и ред уписа нису мењани. Затварање
странице пре одложеног уписа и даље не гарантује ажуран фајл. Два неуспеха
заустављају тај покушај; следећа промена поново покреће чување.
`tools/tests/backup-status.test.mjs` проверава приказ и склопљена подешавања.
`tools/tests/backup-status.browser.js` користи засебне localhost:18764 контексте
и њихов OPFS фајл: стварни упис, одложен close, новију измену и опоравак после
грешке на оба писма. Не бира фајл са корисничког диска.

Од v137 service worker припрема основне ресурсе пре активације. Јавна страница
под његовом контролом користи припремљени HTML, а localhost остаје network-first.
Питања и објашњења доступни су без мреже после успешне инсталације; слике се
кеширају тек по отварању. Прва неконтролисана посета није обухваћена овом гаранцијом.
Не поново користити број издања за другачији садржај: постојеће језгро је непроменљиво.
Повратак старог кода објављује се са **новим, већим бројем**. Ако је стари кеш истог
броја непотпун, нови број омогућава нову припрему без преписивања активног кеша.
Ограничавање старих кешева је још отворен посао. `clients.matchAll()` не види
сваки документ који browser може вратити из BFCache-а; ни две провере нису
безбедна граница за брисање. [А5 одлука и будућа миграција](docs/superpowers/plans/2026-09-10-a5-zadrzavanje-starih-izdanja.md)
бележе стварни `pageshow.persisted` доказ и потребне провере пре чишћења.

Од v157 HTTP слике имају адресу `img/ID.jpg?h=<SHA-256>`. Нови service worker
проверава отисак пре сервирања кешираног/мрежног одговора и пре уписа у
стабилни `va-img-h1`. Промењени бајтови добијају нову адресу; непромењени се
поново користе. Цео URL, укључујући путању fork-а и query, остаје кључ
([Cache.match](https://developer.mozilla.org/en-US/docs/Web/API/Cache/match)).
Неисправан одговор се не кешира; привремени неуспех може поново да се покуша.
Неуспео упис у кеш не одбацује исправан мрежни одговор.

`image-baseline.js` је трајна мапа 704 слике из v153, commit
`40e06b8575875773d66c6c4b54381f1d43014a7c`; бајтови су исти и у v126.
Она служи само старим адресама без query-ја. Стари таб добија своју слику из
кеша или мреже само ако одговара тој мапи; иначе добија грешку уместо друге
слике. Мапа је увезена зависност worker-а, са независно фиксираним отиском у
проверама. **Не регенерисати је и не усклађивати њен обухват са будућом базом.**
Недоступан/неисправан увоз спречава замену активног worker-а. Старе кешеве не бришемо.

Ова провера бајтова важи за захтеве под контролом новог worker-а. Прво
неконтролисано учитавање, преглед без постојећег контролера и удаљени embed
имају означене адресе, али немају додатну проверу бајтова у самом `<img>`.
`file://` задржава адресе без query-ја, јер их локални Chrome иначе не отвара.
Провере отисака су у `image-hashes.test.mjs`; засебни browser runner
`image-hashes.browser.js` проверава HTTP/file приказ и синтетички embed.
За file сценарио caller URL има `?imageFile=file:///.../output/image-hash-a5/index.html`,
путању до сопствене копије јавних runtime фајлова, никад до корисничког checkout-а.

Провере: `node --test tools/tests/service-worker.test.mjs`. За стварни прегледач,
покренути `node tools/fixtures/sw-server.mjs` (засебан порт 18765), затим CLI
`tools/tests/service-worker.browser.js`. Оба контекста и подаци су синтетички;
само fixture мења услов за јавну грану. Продукциони `sw.js` није прилагођаван тесту.
[Cache.addAll](https://developer.mozilla.org/en-US/docs/Web/API/Cache/addAll)
одбија непотпун пакет, али провера/упис у већ активан кеш захтева засебну заштиту.

Садржајна ревизија се води по ID-јевима у `docs/revizija-sadrzaja/`.
Поступак је у [методологији](docs/revizija-sadrzaja/METODOLOGIJA.md).
`node tools/audit-content.mjs --check` проверава потпуност и актуелност евиденције;
`--write` је освежава после прегледа измена; `--require-reviewed` тражи завршене
појединачне прегледе. Почетно је све непрегледано. Алат никада сам не потврђује
тачност наставног текста и не посећује изворе прописа. Провера самог алата:
`node --test tools/tests/content-audit.test.mjs`.

Од v160 `parkiranje-card.test.mjs` проверава знак P, писма/римске ознаке и
усклађеност графикона са стварним кључевима 27 питања. Засебни
`parkiranje-card.browser.js` користи нове localhost контексте за свих 27 питања,
оба писма/теме, 320/1280 px и директни/уграђени појмовник. Проверава приказ,
слике, одговоре и геометрију 19 SVG-ова; не потврђује пуну приступачност.
Од v164 контраст проверавају `parkiranje-text-contrast.browser.js` и
`parkiranje-shape-contrast.browser.js`, у засебним синтетичким localhost
контекстима. [Група 010](docs/revizija-sadrzaja/grupe/010-parkiranje-kontrast.md)
бележи те резултате. Накнадни v170 је поправио ситне натписе у
[групи 011](docs/revizija-sadrzaja/grupe/011-parkiranje-citljivost.md); v171 је
поправио преливање дугачких речи при увећаном тексту. Ови докази не затварају
пуну проверу приступачности или физичких телефона.

Од v165 `tools/tests/image-zoom.browser.js` проверава увећање SVG/JPG слика,
однос страна, расположив простор поред контрола, ротирање, фокус и очување
синтетичког испита. Покренути Playwright CLI-јем над засебном копијом на
localhost:18764; 22 случаја у новим контекстима, уз постојећи `keyboard.browser.js`.
Ова провера не тврди да су ситни натписи у основном приказу решени.

```bash
node -e "new Function(require('fs').readFileSync('app.js','utf8'))"   # синтакса
node tools/build-explanations.mjs                                    # билд + скенер писма
node tools/bump-version.mjs                                          # нова верзија
```

Затим у прегледачу: отвори почетну, уђи у једну област, одговори на питање,
отвори статистику и појмовник. Ако нешто пукне, апликација исписује **црвену
траку са поруком** на врху странице.

## Умеци и плакат

`embed.html` — самостална страница са једним насумичним питањем, за уградњу на сајтове
ауто-школа (учитава `data.js` са јавне адресе). `plakat.html` — А4 плакат са QR кодом.
QR се прави са `tools/napravi-qr.mjs` (без спољних библиотека), а **обавезно се проверава**
са `tools/proveri-qr.mjs` — тај скрипт чита `tools/qr.svg` и мора да испише тачну адресу.
Оба QR алата тренутно захтевају радну фасциклу `tools/` (за разлику од генератора
објашњења). Генератор прави само `qr.svg`; не убацује га сам у `plakat.html`.
При промени адресе засебно заменити SVG у плакату и проверити да је истоветан
провереном `qr.svg`, уз пробу стварним телефоном. Само читање постојећег QR-а
из PowerShell-а, почев из корена пројекта:

```powershell
Push-Location tools
try { node proveri-qr.mjs } finally { Pop-Location }
```

## Права

Питања, одговори и слике су из јавног сервиса еУправе (МУП Србије) и користе се
некомерцијално, за лично учење. Код апликације је ауторски рад власника
репозиторијума. Одлука о лиценци кода и обиму права над МУП материјалом
остаје власнику; јавна доступност није дозвола за сваку даљу употребу.
Проверено објашњење ограничења је у [README.md](README.md#права).

## Мапа стања и места измене

| Податак | Где живи | Пут читања/писања и граница |
|---|---|---|
| Напредак и подешавања `S` | `localStorage`, `vozackiA.v1` | `load` → `normalizeState`; одговор/подешавање → `save`. Извоз и повезани резервни фајл носе овај профил, укључујући завршене испите у `S.sims`. |
| Активан испит `sim` | меморија и засебни `vozackiA.sim` | `simSnimi` чува рок, редослед ID-јева/опција и изборе; `simVrati` проверава запис. `finishSim` бележи завршени исход у профилу. Активан испит није део JSON извоза профила. |
| Право уписа | Web Lock `vozackiA.writer` и провера очекиваних бајтова | Брава се тражи пре учитавања. Стари/сукобљени таб не преузима упис сам; заштита обухвата и резервни фајл. |
| Веза са резервним фајлом | IndexedDB `vozackiA-fs`, store `kv`, кључ `handle` | Чува дозволу/handle, не другу копију напретка. `initBackup`, `scheduleBackup`, `upisiRezervu`; асинхрони упис се не проглашава завршеним пре `close`. |
| Привремени приказ | меморија документа и browser историја | Претрага, скрол, фокус, отворене теме и привремени ред нису напредак. `?preview=1` чита садржај и снимак профила без уписа. |
| Offline ресурси | `va-core-vN`, `va-img-h1`, стари `va-img-1` | Одвојени од `S`/`SIM`. Политика чувања старих издања није брисање напретка; ограничења миграције су у А5 плану. |

Ова мапа не уводи нова поља нити миграцију. Формат и нормализација остају у
`app.js`; старе синтетичке фикстуре су у `tools/tests/` и `tools/fixtures/`.

## Једна измена објашњења, без споредне обнове базе

Пример места измене је постојећи `X[7990]` (дефиниција бицикла). То није налог
да се тај проверени текст мења; поступак важи када постоји конкретна прихваћена
исправка са извором.

1. У корену засебне радне копије пронађи извор: `rg -a -n 'X\[7990\]' tools/build-explanations.mjs`.
   Прочитај све доделе тог ID-ја и завршне обраде; последња додела може надјачати ранију.
2. Забележи пуно старо објашњење оба писма из `EXPLAIN.byQ[7990].x`, питање,
   све опције/кључеве и ефективне картице. Измени само потребан латинични низ у
   генератору, уз очекивани број поклапања и четири очувана NUL бајта.
3. Покрени `node tools/build-explanations.mjs` из корена те радне копије.
   Пре прихватања упореди цео стари/нови излаз: дозвољена садржинска разлика
   за овај пример је само `byQ[7990].x`; датум `updated` разматрај засебно.
   Остала објашњења, картице, `bySub`, атлас, ситуације и замке морају бити једнаки.
   За овакву измену не покретати `build-data.mjs`.
4. Прочитај оба генерисана писма, отвори питање и све зависне делове картица у
   празном контексту. Провери стварни приказ и одговарајуће изворе; синтакса и
   скенер писма не доказују правну тачност. Заврши одговарајућим проверама из
   овог водича и ажурирај појединачну евиденцију прегледа.

Изворна картица није још коначни HTML: стварни редослед је `PO_TEMAMA` →
`capCells`/уклањање коментара → `toCyr` → локалне обраде описа и ознака.
За проверу кандидата извршити цео тај ток. Ако се генератор проверава у VM-у
без стварног уписа, употребити оригинални увезени `SUB_KARTICA_SVE` заједно са
`napraviAtlas`: та функција затвара баш тај објекат. Замена објекта копијом
може тихо уклонити ситуације. Прво доказати да стари генератор даје стварни
runtime излаз, па тек онда поредити кандидата.

`nocard` блокира подразумевану картицу подобласти, не изричиту `card` везу.
Одсуство картице уз питање не искључује појављивање у атласу. Зато при ревизији
картице посебно утврдити директне/подобласне везе, атлас и ситуације.

## Важеће одлуке које не треба поново уводити

- Један извор за испитни шаблон и прагове: `SIM_SLOTS`, изведени `NA_ISPITU`
  и `prag`. У активној симулацији нема објашњења нити помоћи из појмовника.
- Приказ није одговор: читање користи `qr(id)`, а не `qs(id)` који ствара запис.
  Дневна ауто-квота не сме да се помера због сваког одговора; датум и намерна
  промена плана имају засебан животни циклус. Непозната спремност није нула
  нити измерена вероватноћа положеног испита.
- Навигација користи стварне адресе; обичан клик, нови таб, Назад/Напред и
  директно отварање морају сачувати уговор прегледа. Привремени `#/vezba` ред
  не преживљава нови документ као записани напредак. Преглед старог испита
  идентификује конкретан садржај покушаја; његов отисак се не шаље аналитици.
  Провере користе исти изоловани CLI поступак: `navigation.browser.js`,
  `navigation-links.browser.js`, `statistics-preview.browser.js`,
  `personal-lists-preview.browser.js` и `history-preview.browser.js` у `tools/tests/`.
- У картици се теме деле само према актуелном `PO_TEMAMA`. Атлас и ситуације
  се стварају лењо; `suziKarticu` их не уклања. Варијанте слика са истим
  значењем се чувају, а натписи се читају из сачуване базе, не представљају
  сами по себи нову проверу важећег прописа.
- Увећање цртежа је статички SVG у `<img>`: `crtezUSliku` уграђује боју и
  породицу фонта, а не CSS анимације. Контроле покрета у основној картици су
  засебан отворен задатак А4/Б1; системски `prefers-reduced-motion` већ ради.

Разлози, ранија мерења и напуштена решења су сачувани у
[RAZVOJ-ISTORIJA.md](RAZVOJ-ISTORIJA.md). Тај запис се чита уз наведено издање;
актуелни статус пакета је у [IZVRSAVANJE-PLANA.md](IZVRSAVANJE-PLANA.md), а
садржински статус по ID-ју у [евиденцији ревизије](docs/revizija-sadrzaja/manifest.json).
